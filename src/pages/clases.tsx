import * as React from "react";
import Layout from "../components/Layout";

const Clases = () => {
  return (
    <Layout>
      <div className="min-h-screen flex items-center">
        <div className="container">Clases</div>
      </div>
    </Layout>
  );
};

export default Clases;
